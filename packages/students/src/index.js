import React from 'react';
import ReactDOM from 'react-dom';

import { TestComponent } from 'delta-common';

ReactDOM.render(
  <h1>Welcome to Project Delta Students <TestComponent /></h1>,
  document.getElementById('root'),
);
